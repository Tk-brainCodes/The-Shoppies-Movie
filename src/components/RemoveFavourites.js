import React, { Fragment} from 'react';

const RemoveFavourites = () => {
	return (
		<Fragment>
			<button><span className='mr-2'>Remove</span></button>
		</Fragment>
	);
};

export default RemoveFavourites;
